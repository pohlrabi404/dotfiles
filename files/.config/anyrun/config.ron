Config(
  x: Fraction(0.5),

  y: Fraction(0.2),

  width: Absolute(500),

  height: Absolute(0),
  
  hide_icons: false, 

  ignore_exclusive_zones: false, 

  layer: Overlay, 
  
  hide_plugin_info: true, 

  close_on_click: false,

  show_results_immediately: true,

  max_entries: None,
  
  plugins: [
    "libapplications.so",
    "libdictionary.so",
    "libwebsearch.so",
    "libshell.so",
    "libtranslate.so",
    "libstdin.so",
    ]
)
