;; theme
(setq inhibit-startup-message t)

;; unnecessary bars shii
(menu-bar-mode -1)
(tool-bar-mode -1)
(scroll-bar-mode -1)

;; relative line number
(global-display-line-numbers-mode 1)
(setq display-line-numbers 'relative)
